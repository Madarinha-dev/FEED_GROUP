# Importa o objeto 'app' do seu arquivo principal.
# Se o seu arquivo principal se chamar 'main.py' ou outro nome,
# você precisará mudar a linha abaixo.
from back import app as application

# O nome da variável precisa ser 'application' para que o servidor da Hostinger
# consiga encontrá-la e iniciar a sua aplicação