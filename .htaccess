# Ativa o motor de reescrita
RewriteEngine On

# Redireciona todas as requisições para o arquivo passenger_wsgi.py
# Isso é essencial para que o servidor consiga rodar a sua aplicação Python
RewriteRule ^(.*)$ passenger_wsgi.py/$1 [QSA,L]

# Define o manipulador para o Python 3.9. 
# Se a sua aplicação usar uma versão diferente, você precisará ajustar aqui.
SetHandler "application/x-httpd-ea-cgi-python-3.9"